<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".POSApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SeaCliffPOS"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Order Activity -->
        <activity
            android:name=".ui.activities.OrderActivity"
            android:exported="false"
            android:parentActivityName=".ui.activities.MainActivity" />

        <!-- Tables Activity -->
        <activity
            android:name=".ui.activities.TablesActivity"
            android:exported="false"
            android:parentActivityName=".ui.activities.MainActivity" />

        <!-- Payment Activity -->
        <activity
            android:name=".ui.activities.PaymentActivity"
            android:exported="false"
            android:parentActivityName=".ui.activities.OrderActivity" />

        <!-- Orders Activity -->
        <activity
            android:name=".ui.activities.OrdersActivity"
            android:exported="false"
            android:parentActivityName=".ui.activities.MainActivity" />

        <!-- Menu Activity -->
        <activity
            android:name=".ui.activities.MenuActivity"
            android:exported="false"
            android:parentActivityName=".ui.activities.OrderActivity" />

        <!-- Login Activity -->
        <activity
            android:name=".ui.activities.LoginActivity"
            android:exported="false" />

        <!-- Order Details Activity -->
        <activity
            android:name=".ui.activities.OrderDetailsActivity"
            android:exported="false"
            android:parentActivityName=".ui.activities.OrdersActivity" />

        <!-- Tips Activity -->
        <activity
            android:name=".ui.activities.TipsActivity"
            android:exported="false"
            android:parentActivityName=".ui.activities.MainActivity" />

        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name=".service.PosFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- WorkManager - Using on-demand initialization via Configuration.Provider -->
        <!-- Disable WorkManager auto-initialization -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="remove" />

    </application>

</manifest>
