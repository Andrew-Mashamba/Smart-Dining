# Android SDK Location
# Update this to your Android SDK path
sdk.dir=/Users/YOUR_USERNAME/Library/Android/sdk

# API Configuration
# Choose the appropriate configuration based on your development setup:

# Option 1: Android Emulator (Recommended for development)
# Use 10.0.2.2 to access host machine's localhost from emulator
api.base.url="http://10.0.2.2:8000/api/"

# Option 2: Physical Android Device on same WiFi
# Replace 192.168.1.105 with your Mac's actual IP address
# Find IP: ipconfig getifaddr en0
#api.base.url="http://192.168.1.105:8000/api/"

# Option 3: Production Server
#api.base.url="https://api.seacliff.com/api/"

# API Settings
api.timeout=30          # Request timeout in seconds
sync.interval=300       # Background sync interval in seconds (5 minutes)

# Before running the app, make sure Laravel backend is running:
# cd ../laravel-app
# php artisan serve --host=0.0.0.0 --port=8000
